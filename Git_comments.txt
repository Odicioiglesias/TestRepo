# Keys words used with GitHub:

git status # current status of repository

git add -A # adds altered und untracked files to staging area

git commit -am '<message>' #commit changes with message

git push # push committed up to GitHub
git pull # pull any changes from GitHub down to local repository